import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import {Screen, Block} from '@components';

export const HomeTab = () => {
  return (
    <Block block>
      <Screen backgroundColor={'#5c3cb9'}></Screen>
    </Block>
  );
};

const styles = StyleSheet.create({});
