import { Colors } from '@theme';

export interface DividerProps {
  /**
   * Overwrite color with theme
   */
  colorTheme?: Colors;

  /**
   * Height of divider
   * @default 1
   */
  height?: number;
}
