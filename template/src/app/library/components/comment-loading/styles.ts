import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  row: {
    marginBottom: 10,
    flexDirection: 'row',
  },
  wrap: {
    flex: 1,
    height: '100%',
    paddingHorizontal: 20,
  },
});
