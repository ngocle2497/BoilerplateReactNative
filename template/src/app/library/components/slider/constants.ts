const ACTIVE_COLOR = '#0057e7';

const IN_ACTIVE_COLOR = '#dfe6e9';

const HEIGHT_SLIDER = 3;

const THUMB_SIZE = 15;

const LOWER_BOUND = 0;

const UPPER_BOUND = 100;

const FIXED_AFTER = 1;

const INITIAL_LINEAR = 0;

const INITIAL_RANGE = [0, 10] as [number, number];

export {
  ACTIVE_COLOR,
  IN_ACTIVE_COLOR,
  HEIGHT_SLIDER,
  THUMB_SIZE,
  LOWER_BOUND,
  UPPER_BOUND,
  INITIAL_LINEAR,
  INITIAL_RANGE,
  FIXED_AFTER,
};
